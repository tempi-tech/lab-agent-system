[pytest]
# Only collect tests under ./tests.
# This prevents accidental collection of utility scripts (e.g. scripts/smoke_test.py),
# which may require env/network and may mutate process environment at import time.
testpaths = tests
python_files = test_*.py

