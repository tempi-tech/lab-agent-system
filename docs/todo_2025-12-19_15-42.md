# ToDo 2025-12-19 15:42（整理のみ）

## 目的
- **明日**は `quiz_master` と `invite_role_assigner` を **同時稼働**させる。
- クイズは **4択ではなく自由入力**に変更し、
  知識問題も **LLMで正誤判定**できる形式にする。
- 3問構成に変更（下記）。

---

## 今日決まった問題（3問構成）
1) **知識（自由入力）**
   - お題: EQベンチで1位のモデルは？
   - 参考: https://eqbench.com/
   - 想定正解: `Kimi-K2-Instruct`
   - 形式: 選択式ではなく自由入力（LLMで正誤判定）

2) **知識（自由入力）**
   - お題: 「サムアルトマンの自伝」からの内容で、引っ掛からなそうな問題
   - 例: 幼少期のエピソード／最初のスタートアップなど
   - 形式: 自由入力（LLMで正誤判定）

3) **クリエイティブ（正解なし）**
   - お題を決めて AIの回答で対決
   - 採点は LLM（すでに実装済み）

---

## 対応が必要なToDo（実装計画は別途）
### A. 運用
- **Dockerを再起動し、明日は2エージェント同時稼働**
  - `quiz_master`
  - `invite_role_assigner`

### B. クイズ問題の変更
- 4択形式 → **自由入力形式へ変更**
- EQベンチ問題・サムアルトマン問題・クリエイティブ問題の **3問構成**に変更

### C. 採点方式の変更（知識問題）
- 現状は **選択式／正解一致**のみ
- **LLMで正誤判定できる仕組み**へ移行

---

## 影響があるファイル（該当箇所）

### 1) QuizMaster 本体
- `src/agents/quiz_master/agent.py`
  - 出題処理: `_post_next_question()`
  - 回答収集: `_capture_submission()`
  - 採点: `_close_and_grade()` → `_grade_knowledge()` / `score_creative_answers()`
  - 運営コマンド: `on_message()` / `_handle_admin_command()`

### 2) 採点ロジック
- `src/agents/quiz_master/scoring.py`
  - クリエイティブ採点: `score_creative_answers()`
  - Claude/Gemini 切替: `ClaudeLLM` / `GeminiLLM`
  - **知識問題のLLM採点はまだ未実装**

### 3) 問題データ（JSON）
- `src/agents/quiz_master/quizzes/bonenkai_2025.json`
  - 現在: 4択（knowledge） + クリエイティブ2問
  - **ここを3問構成に変更**（自由入力）

### 4) 環境変数（運用設定）
- `.env` / `.env.example`
  - `QUIZ_ADMIN_CHANNEL_ID`
  - `QUIZ_FORUM_CHANNEL_ID`
  - `QUIZ_LLM_PROVIDER`（claude/gemini）
  - `ANTHROPIC_API_KEY`（Claude採点）

### 5) InviteRoleAssigner
- `src/agents/invite_role_assigner/logic.py`
- `src/agents/invite_role_assigner/config.py`
  - **必須env**が揃っていないと起動失敗するため、
    本番Docker起動前に env が揃っているか確認が必要

### 6) 起動経路
- `main.py`
  - 通常起動（Docker）: `quiz_master` + `invite_role_assigner` を登録
  - `--once`: DailyReporter のみ

---

## ディレクトリ全体像（該当部分）
```
src/
├── agents/
│   ├── quiz_master/
│   │   ├── agent.py          # 出題・採点ロジック
│   │   ├── scoring.py        # LLM採点
│   │   ├── storage.py        # 状態保存
│   │   ├── utils.py          # コマンド解析
│   │   └── quizzes/*.json    # 問題データ
│   ├── invite_role_assigner/ # 招待リンクロール付与
│   └── daily_reporter/       # 日報生成（--once）
└── core/
    ├── bot.py                # CommunityBot
    └── config.py             # 環境変数

docs/
├── quiz_forum_overview.md
├── quiz_forum_implementation_log_2025-12-19.md
├── quiz_forum_inspect.md
└── forum_quiz_troubleshooting.md
```

---

## 関連ドキュメント
- `docs/quiz_forum_overview.md`
- `docs/quiz_forum_inspect.md`
- `docs/forum_quiz_troubleshooting.md`
